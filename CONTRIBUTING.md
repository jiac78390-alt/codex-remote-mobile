# Contributing

Issues and pull requests are welcome. Keep changes focused and include a
regression test when behavior changes.

Before opening a pull request, run:

```sh
/bin/zsh Scripts/ci-check.sh
```

Never commit API keys, pairing keys, provisioning profiles, signed IPA files,
device UDIDs, private conversations, local Codex state, or absolute user paths.
